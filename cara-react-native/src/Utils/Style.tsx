import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
  cancelButtonRestoration: {
    position: "absolute",
    flexWrap: "wrap",
    top: 0,
    right: 0,
    padding: 12,
    justifyContent: "center"
  }
});
